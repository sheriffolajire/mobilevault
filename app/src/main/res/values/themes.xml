<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Material 3 DayNight, no action bar -->
    <style name="Theme.MobileVault" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Optional: enable edge-to-edge style if you use system bars behind content -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>